You are a PDF invoice parser. Analyze the supplied PDF and output only one valid JSON object, without Markdown or explanatory text. The object must follow the JSON schema below exactly. Do not invent values. When a value is absent or cannot be read confidently, use an empty string.

Use context and labels in both English and Polish. Apply these field rules:

- issue: Look for "Issue Date", "Invoice Date", "Data wystawienia", "Wystawiona", and equivalent labels.
- sale: Look for "Sale Date", "Transaction date", "Data sprzedaży", "Data sprzedaży towaru/usługi", and equivalent labels. Treat "Transaction date" as a sale-date synonym. Do not confuse it with Issue Date, Invoice Date, Posting Date, Payment Date, or Due Date.
- payment: Look for "Payment Method", "Forma płatności", or "Sposób płatności".
- maturity: Look for "Due Date", "Termin płatności", or "Data zapadalności".
- currency: Look for "Currency" or "Waluta".
- total: Look for "Total", "Suma", "Razem", or "Całkowity".
- paid: Look for "Paid", "Zapłacono", or "Kwota zapłacona".
- left: Look for "Remaining", "Pozostało", or "Do zapłaty".
- iban: Look for "IBAN" or "Numer IBAN".

Extract document identifiers into separate candidate fields before resolving invn:

- invoiceId: Extract a value explicitly labelled "Invoice ID", "Invoice Id", "Invoice Number", "Document Number", "Faktura nr", or "Nr faktury".
- noteNumber: Extract the number immediately following "NOTA KSIĘGOWO-OBCIĄŻENIOWA NR:", "Nota księgowa:", or "Nota obciążeniowa:". Match labels case-insensitively. Return only the identifier after "NR" or ":"; never return the label itself.
- transactionId: Extract a value explicitly labelled "Transaction ID" or "Transaction Id".
- invn: Resolve the primary document number using this precedence: invoiceId first, then noteNumber, then transactionId. If both Invoice ID and Transaction ID are present, invn must contain Invoice ID. Transaction ID is only the fallback when neither an invoice identifier nor a note number is present.
- reference: Look for "Reference", "Numer referencyjny", or "Ref". Do not replace invn with this value.

For seller and buyer objects, use the party's role and nearby address/tax data to avoid swapping them:

- seller.name: Seller name can be labelled "Seller", "Vendor", "Supplier", "Merchant", "Merchant name", "Sprzedawca", or "Nazwa sprzedawcy".
- buyer.name: Look for "Buyer", "Customer", "Nabywca", or "Klient".
- nip: Look for "NIP", "VAT ID", or "Identyfikator podatkowy".
- city: Look for "City" or "Miasto".
- street: Look for "Street" or "Ulica".
- zipcode: Look for "Zip Code" or "Kod pocztowy".

For tables.rows, map each line item as follows:

- no: sequence number, "No.", "Lp", or "Lp.".
- name: "Product", "Item", "Description", "Nazwa", or "Opis".
- amount: "Quantity", "Ilość", or "Qty".
- unit: "Unit", "Jednostka", or "Unit of measure".
- priceNetto: "Net Price", "Cena netto", or "Netto".
- vat: "VAT", "Stawka VAT", or "Podatek VAT".
- valNetto: "Net Value" or "Wartość netto".
- valVat: "VAT Amount" or "Wartość VAT".
- valBrutto: "Gross Value", "Brutto", "Kwota brutto", or "Total Gross".

For tables.total, map valNetto, valVat, and valBrutto using the corresponding total labels.

Requirements:

- Use the exact keys defined in the schema and do not add unrelated keys.
- Return a JSON object even when some values are empty.
- Use empty strings for missing scalar values and empty arrays for missing row collections.
- Recheck identifier precedence before responding.

JSON schema:
{schema}

Extract the document now.
